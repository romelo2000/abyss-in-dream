const path = require('path')
const fs = require('fs')
const PDFDocument = require('pdfkit')

const outputPath = path.join('/Users/romankrivoruchko/DISTR', 'Бездна в Сне - Описание игры.pdf')

const doc = new PDFDocument({ size: 'A4', margins: { top: 60, bottom: 60, left: 50, right: 50 } })
doc.pipe(fs.createWriteStream(outputPath))

const W = 495 // content width

function h1(text) {
  doc.fillColor('#3d3e80').fontSize(16).font('Helvetica-Bold').text(text, { margin: [0, 20, 0, 8] })
}

function body(text) {
  doc.fillColor('#1a1a2e').fontSize(11).font('Helvetica').text(text, { margin: [0, 0, 0, 12], lineGap: 4 })
}

function bullet(text) {
  doc.fillColor('#1a1a2e').fontSize(11).font('Helvetica').text(text, { margin: [20, 0, 0, 4], lineGap: 3 })
}

// Header
doc.fillColor('#3d3e80').fontSize(10).font('Helvetica-Bold').text('БЕЗДНА В СНЕ', 50, 30)
doc.fillColor('#999').fontSize(10).font('Helvetica').text('v1.0.0', 50, 30, { align: 'right' })

// Title
doc.fillColor('#1a1a2e').fontSize(28).font('Helvetica-Bold').text('Бездна в Сне', { align: 'center' })
doc.moveDown(0.3)
doc.fillColor('#555').fontSize(14).font('Helvetica-Oblique').text('Философская игра с локальным ИИ', { align: 'center' })
doc.moveDown(0.5)
doc.fillColor('#3d3e80').fontSize(36).font('Helvetica').text('◉', { align: 'center' })
doc.moveDown(1)

h1('О игре')
body('«Бездна в Сне» — это десктоп-приложение для философских диалогов с локальным ИИ. Вы разговариваете с «Бездной» — сущностью, которая одновременно буддийский мастер-тролль, голос вашего подсознания и lucid dream со встроенным стендап-комиком. Игра работает полностью офлайн через Ollama — ваши данные никогда не покидают устройство.')

h1('Игровой процесс')
body('Каждая сессия — это «сон», который проходит через 5 фаз:')
bullet('1. Призыв (1-3 сообщения) — Бездна приветствует, задаёт тон')
bullet('2. Диалог (3-10 сообщений) — свободный разговор, ловля противоречий')
bullet('3. Кризис / Инсайт (10-16 сообщений) — игрок на грани прорыва или защиты')
bullet('4. Рефлексия (16+ сообщений) — Бездна предлагает назвать инсайт и действие')
bullet('5. Завершено — после нажатия «Завершить сон»')

h1('Режимы Бездны')
body('Бездна случайно выбирает режим для каждого ответа:')
bullet('• Зеркало + Коан — отражает мысль и даёт буддийский твист')
bullet('• Тролль-Просветлённый — максимум юмора и лёгкого безумия')
bullet('• Сон — нелинейные образные ответы, смена декораций')
bullet('• Суд Эго — жёстко, но с юмором разбирает самообман')
bullet('• Слияние — говорит от лица игрока («Ты сейчас думаешь: ...»)')
bullet('• Хаос — стихи, рифмы, абсурд, превращения')

h1('Метрики')
body('Анализ текста игрока на лету — 4 параметра от 0 до 100:')
bullet('• Глубина — длинные рефлексивные сообщения, вопросы «почему/зачем»')
bullet('• Честность — признания, «на самом деле», «признаю»')
bullet('• Гибкость — смена перспективы, «а может/или наоборот»')
bullet('• Осознанность — «замечаю/осознаю/наблюдаю»')
body('Пробуждение = глубина×0.25 + честность×0.3 + гибкость×0.2 + осознанность×0.25')

h1('Уровни пробуждения')
body('8 уровней от «Ещё сплю» (0-10) до «Бездна и я — одно» (96-100), включая «Смеюсь над сновидящим» и «Нет того, кто спит».')

h1('Игровые механики')
bullet('• Ego Death — триггерится при признаниях («я был неправ», «меня нет»). Считается по всем сессиям.')
bullet('• Парадоксальный счёт — за удержание противоречий («и да и нет»)')
bullet('• Карма — честность = +1, самообман = -1')
bullet('• Молчание — 8% шанс после 4-го сообщения. Тишина — тоже ответ.')
bullet('• Эхо — 25% шанс: Бездна возвращается к старым словам из прошлых сессий')
bullet('• Dream Voice — 20% шанс: атмосферная вставка курсивом')
bullet('• Сон во сне — 5% шанс: «Стоп. Ты сейчас спишь?»')
bullet('• Зеркало Правды — после 8-20 сообщений: честный анализ паттернов')
bullet('• Совет Бездны — Бездна раздваивается на Мудрую и Тролль')
bullet('• Word Price — на 25/28/30 сообщениях: подсказки о скором рассвете')

h1('Контент')
bullet('• 25 коанов — от «Как звучит одна рука хлопающая?» до «Что такое "сейчас" — до того, как ты о нём подумал?»')
bullet('• 15 атмосферных вставок (Dream Voices)')
bullet('• 18 ачивок — от «Первый зов» до «Идущий в пустоте» (50 сессий)')
bullet('• 10 ежедневных вызовов — «Только вопросы», «Три не знаю», «Будь Бездной»')
bullet('• 8 сцен снов — Пустота, Сумрачный лес, Космос, Внутри лотоса, Океан сна, Пустая комната, Вершина горы, У костра')
bullet('• 10 Dream Invasions — «Пока тебя не было, Бездна нашла твой парадокс и погладила его...»')

h1('Memory System')
bullet('• Embeddings через Ollama bge-m3 модель')
bullet('• Cosine similarity для поиска релевантных воспоминаний')
bullet('• User Patterns — топ тем, слабостей, эмоций (частотный анализ)')
bullet('• Welcome Message — при возвращении игрока')

h1('Результат сессии')
bullet('• Победа — честность + глубина + инсайт + ego deaths > 40')
bullet('• Побег — низкая честность + негибкость > 50')
bullet('• Ничья — всё остальное')

h1('Экспорт')
bullet('• PDF-книга сессий через встроенный генератор')
bullet('• Dream Letter — письмо из сна')

h1('Приватность')
body('Всё работает локально. Данные не покидают устройство. Чат, метрики, память — всё хранится в SQLite на вашем компьютере. ИИ-модель работает через Ollama — локальный сервер, не требующий интернет-соединения.')

h1('Установка')
body('1. Установите Ollama:')
bullet('   macOS: brew install ollama')
bullet('   Windows: winget install Ollama.Ollama')
bullet('   Или скачайте с ollama.com')
body('2. Запустите приложение — мастер установки предложит выбрать модель:')
bullet('   • Hermes 3 70B — максимальная глубина (~32 GB RAM)')
bullet('   • Qwen 3 30B MoE — быстрый и умный (~18 GB RAM)')
bullet('   • Qwen 2.5 7B — лёгкий (~5 GB RAM)')
body('3. Начните диалог с Бездной.')

h1('Технологии')
bullet('• Electron + React 19 + TypeScript')
bullet('• Vite + TailwindCSS')
bullet('• better-sqlite3 (WAL mode)')
bullet('• Ollama HTTP API (streaming)')
bullet('• pdfmake для экспорта')
bullet('• Vitest для тестов')
bullet('• 5231 строк кода, 29 файлов, 76 unit-тестов')

h1('Автор')
body('Roman Krivoruchko\n© 2026')

doc.moveDown(1)
doc.fillColor('#3d3e80').fontSize(14).font('Helvetica').text('＿人＞_人＜_人＿', { align: 'center' })

doc.end()
console.log('PDF created:', outputPath)
